Pips Master Academy — Blank Sections Fix

Root cause found: the main page router referenced progress() and referrals() render functions that were missing from the deployed frontend. That caused the router to stop before rendering the Home/other sections after login.

Fix applied:
- Restored My Progress and Referrals page renderers.
- Restored progress loading and completion actions for lessons/tasks/daily tasks.
- Added missing completed lesson/task IDs to the backend /api/progress response.
- Added explicit safe DOM references for the user name, page title and screen renderer.
- Applied the same frontend repair to app.js, frontend/app.js and frontend_embedded.js.
- Authentication/login function was not changed.
- Backend app.py and market_data.py compile successfully.
- All JavaScript files pass node --check.
