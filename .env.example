PMA_DB=pma.db
# Set this to true ONCE on a fresh deployment if the admin email is stuck in an old database.
# The backend marks the reset as completed, so it will not delete the admin again on restart.
RESET_ADMIN_ACCOUNT_ONCE=true
